#!/usr/bin/env bash
# debug-anastasia-container
# create an anastasia container and run a bash shell

docker run \
    --entrypoint bash \
    --interactive \
    --name=debug_me \
    --rm \
    --tty \
    anastasia:latest
